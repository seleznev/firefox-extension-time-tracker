Time Tracker = Time Tracker

Issue = Issue #%d
total = total: %d

Start tracking = Start tracking
Stop tracking = Stop tracking
Mark as reported = Mark as reported
Copy duration = Copy duration

Mark as reported and copy duration into form on the page = Mark as reported and copy duration to field on the page
You can use Ctrl key to invert action = You can use Ctrl key to invert action
Copy duration of current task to the clipboard = Copy duration of current task to the clipboard
